// Predict the output.
// int main()
// {
// int sub[50], i ;
// for ( i = 0 ; i <= 48 ; i++ ) ;
// {
// sub[i] = i ;
// cout<<sub[i]<<endl ;
// }
// return
// }

// The for loop runs from i = 0 to i = 48, but since the loop body is empty due to the semicolon, no operations are performed within the loop.
// After the loop finishes, i will be 49 (the value after the loop exits).
// The code block { sub[i] = i; cout << sub[i] << endl; } is executed only once after the loop completes, with i being 49.